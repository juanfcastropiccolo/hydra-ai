# QA manual — Feature 007 · Config

Build: `release/mac/Hydra.app` (v0.6.0, x64) · 2026-08-26
Leyenda: 🧪 unit/integración/E2E automatizados · 🤖 verificado por Claude sobre la app compilada (capturas de las 6 categorías) · ⏳ pendiente de Juan

| AC    | Qué                                                                           | Estado  | Notas                                                                                                          |
| ----- | ----------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------- |
| AC-1  | Abrir por ítem y ⌘,; volver sin re-attach                                     | 🧪 + 🤖 | `e2e/09` (mismos pids de PTY); menú "Preferencias… (⌘,)"                                                       |
| AC-2  | Guardado instantáneo (toggle → `hydra.json` < 1 s; persiste al reiniciar)     | 🧪      | `e2e/09` (`prefs.get`), unit `ProjectStore.setPrefs`                                                           |
| AC-3  | Barra "cambios sin guardar" en Perfil; Guardar/Descartar; avatar en vivo      | 🧪 + 🤖 | `e2e/09`: nombre "Zoe Ramírez" → avatar "ZR" en el sidebar                                                     |
| AC-4  | Fuente de terminal y acento en vivo, sin re-attach                            | 🧪      | `e2e/09`: `data-font-size` 13→16 con los mismos pids; `--accent` pasa a violeta                                |
| AC-5  | Flags al crear sesión (`--model haiku --effort low`)                          | 🧪      | `e2e/09` (fake CLI registra las opciones), unit `spawnBackground`                                              |
| AC-6  | Tabla de precios: editar/agregar/quitar/restaurar; recalcula sin reindexar    | ⏳      | **Juan:** subí el precio de salida de Fable, guardá, mirá Analytics; después "Restaurar defaults"              |
| AC-7  | Cambiar puerto MCP reinicia y re-registra; puerto tomado → rechazado          | 🧪 + ⏳ | integración `switchMcpPort`; **Juan:** con MCP conectado, cambiá el puerto y corré `claude mcp get hydra-know` |
| AC-8  | Mantenimiento: CLI, tamaños, errores, borrar fichas, export/import con `.bak` | 🧪 + ⏳ | `e2e/09` (borrar fichas, versión del CLI), unit `importHydraJson`; **Juan:** exportá e importá de verdad       |
| AC-9  | Validación inline (puerto 80, iniciales de 4+)                                | 🧪      | `e2e/09`                                                                                                       |
| AC-10 | No rompe 001–006                                                              | 🧪      | 18 E2E + unit verdes; canales `ui.getX/setX` viejos retirados a favor de `prefs.*`                             |
| AC-2' | Look "tipo Discord"                                                           | 🤖 + ⏳ | Capturas revisadas: nav izquierda con grupos, filas con label+ayuda+control, toasts. **Juan: veredicto**       |

## Checklist manual para Juan

1. Abrí **Config** (o ⌘,). ¿Se siente "Discord"? Recorré las 6 categorías.
2. **Perfil:** poné tu nombre e iniciales → ¿aparecen en el avatar del sidebar al Guardar?
3. **Apariencia:** subí la fuente de las terminales y cambiá el acento → ¿cambia en vivo, sin que las terminales se reinicien? Elegí el acento que más te guste.
4. **Sesiones:** elegí modelo/esfuerzo y creá una sesión nueva → en su TUI (`/status` o la barra) debería verse el modelo elegido. Probá el patrón de nombre con `{date}`.
5. **Contexto e IA:** cambiá el puerto MCP con el MCP conectado → `claude mcp get hydra-know` muestra la URL nueva. Probá un puerto tomado (p. ej. el de otra app) → se rechaza.
6. **Analytics:** editá un precio y mirá que el costo cambie en el dashboard; "Restaurar defaults".
7. **Mantenimiento:** Exportar → Importar el mismo archivo → todo igual y existe `hydra.json.bak`; "Volver a detectar" el CLI; "Abrir en Finder".

## Resultado QA de Juan

_(pendiente)_
